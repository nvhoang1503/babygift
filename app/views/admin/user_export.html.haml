
%table{:border => "1"}
  %tr{:style => 'font-weight: bold;'}
    %th.text.bold.font26 No
    %th Email
    %th Join Date
  - @users.each_with_index do |user,index|
    %tr
      %td= index + 1
      %td= user.email
      %td= user.created_at.strftime("%m/%d/%Y")
